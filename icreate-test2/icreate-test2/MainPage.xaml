<common:LayoutAwarePage
    x:Name="pageRoot"
    x:Class="icreate_test2.MainPage"
    DataContext="{Binding DefaultViewModel, RelativeSource={RelativeSource Self}}"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:icreate_test2"
    xmlns:common="using:icreate_test2.Common"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Page.Resources>

        <!-- TODO: Delete this line if the key AppName is declared in App.xaml -->
        <x:String x:Key="AppName">My Application</x:String>
    </Page.Resources>

    <!--
        This grid acts as a root panel for the page that defines two rows:
        * Row 0 contains the back button and page title
        * Row 1 contains the rest of the page layout
    -->

    <Grid Style="{StaticResource LayoutRootStyle}">
        <Grid.RowDefinitions>
            <RowDefinition Height="768"/>
        </Grid.RowDefinitions>
            <Viewbox MaxHeight="768" MaxWidth="1024">
                <Canvas Height="768" Width="1024" >
                    <Canvas.Resources>
                        <Storyboard x:Name="myStoryboard"> 
                            <DoubleAnimationUsingKeyFrames Storyboard.TargetName="MyAnimatedTranslateTransform" Storyboard.TargetProperty="X" Duration="0:0:3">
                            <LinearDoubleKeyFrame Value="0" KeyTime="0:0:1" />
                            <LinearDoubleKeyFrame Value="-250" KeyTime="0:0:2" />
                            </DoubleAnimationUsingKeyFrames>
                        </Storyboard>
                    </Canvas.Resources>
                    <Image Source="Assets/SplashScreen.png"  Margin="202,234,202,234">
                        <Image.RenderTransform>
                            <TranslateTransform x:Name="MyAnimatedTranslateTransform" X="0" Y="0" />
                        </Image.RenderTransform>
                    </Image>
                </Canvas>
            </Viewbox>
        <Viewbox MaxHeight="768" MaxWidth="1366">
            <StackPanel>
                <StackPanel.Resources>
                    <Storyboard x:Name="showUserInfo">
                        <FadeInThemeAnimation  Storyboard.TargetName="userInfo" Duration="0:0:0.6"/>
                    </Storyboard>
                    <Storyboard x:Name="hideUserInfo">
                        <FadeOutThemeAnimation  Storyboard.TargetName="userInfo" />
                    </Storyboard>
                    <Storyboard x:Name="userInfoOpacity">
                        <DoubleAnimation Storyboard.TargetName="userInfo" Storyboard.TargetProperty="(Rectangle.Opacity)" From="0.0" To="1.0" Duration="0:0:5" />
                    </Storyboard>
                </StackPanel.Resources>
                <StackPanel Name="userInfo" Margin="1000,234,100,234" >
                    <StackPanel  Orientation="Horizontal" Margin="0,10,0,10">
                        <TextBlock Text="UserID" FontSize="42" Margin="0,4,0,4"/>
                    <Border Margin="150,0,0,0" BorderBrush="Gray" BorderThickness="4">
                            <TextBox Name="userID"  Width="300" FontSize="30" TextChanged="userIDChanged" />
                        </Border>
                    </StackPanel>
                    <StackPanel  Orientation="Horizontal" Margin="0,10,0,10">
                    <TextBlock Text="Password" FontSize="42" Margin="0,4,0,4"/>
                    <Border Margin="101,0,0,0" BorderBrush="Gray" BorderThickness="4">
                            <PasswordBox Name="password"  Width="300" FontSize="30" PasswordChar="*" PasswordChanged="passwordChanged" />
                        </Border>
                    </StackPanel>
                    <StackPanel  Orientation="Horizontal" Margin="0,10,0,10">
                    <TextBlock Text="Domain" FontSize="42" Margin="0,4,0,4"/>
                    <Border Margin="128,0,0,0" BorderBrush="Gray" BorderThickness="4">
                            <ComboBox x:Name="domain" SelectedItem="domainItem"  Width="200" FontSize="30" SelectionChanged="domainSelectionChanged">
                                <x:String>NUSSTU</x:String>
                                <x:String>NUSSTF</x:String>
                                <x:String>NUSSSS</x:String>
                            </ComboBox>
                        </Border>
                    </StackPanel>
                    <StackPanel Margin="0,40,0,10">
                        <Border Margin="228,0,230,0" BorderBrush="Gray" BorderThickness="4">
                            <Button Content="Login" FontSize="35" Click="login_Click"/>
                        </Border>
                    </StackPanel>
                </StackPanel>
            </StackPanel>
        </Viewbox>
        <Viewbox MaxHeight="768" MaxWidth="1024">
            <StackPanel Margin="0,50,0,400">
                <ProgressRing x:Name="progressRing" IsActive="False" Height="100" Width="100"/>
            </StackPanel>
        </Viewbox>
        <VisualStateManager.VisualStateGroups>

            <!-- Visual states reflect the application's view state -->
            <VisualStateGroup x:Name="ApplicationViewStates">
                <VisualState x:Name="FullScreenLandscape"/>
                <VisualState x:Name="Filled"/>

                <!-- The entire page respects the narrower 100-pixel margin convention for portrait -->
                <VisualState x:Name="FullScreenPortrait">
                    <Storyboard>
                        <ObjectAnimationUsingKeyFrames Storyboard.TargetName="backButton" Storyboard.TargetProperty="Style">
                            <DiscreteObjectKeyFrame KeyTime="0" Value="{StaticResource PortraitBackButtonStyle}"/>
                        </ObjectAnimationUsingKeyFrames>
                    </Storyboard>
                </VisualState>

                <!-- The back button and title have different styles when snapped -->
                <VisualState x:Name="Snapped">
                    <Storyboard>
                        <ObjectAnimationUsingKeyFrames Storyboard.TargetName="backButton" Storyboard.TargetProperty="Style">
                            <DiscreteObjectKeyFrame KeyTime="0" Value="{StaticResource SnappedBackButtonStyle}"/>
                        </ObjectAnimationUsingKeyFrames>
                        <ObjectAnimationUsingKeyFrames Storyboard.TargetName="pageTitle" Storyboard.TargetProperty="Style">
                            <DiscreteObjectKeyFrame KeyTime="0" Value="{StaticResource SnappedPageHeaderTextStyle}"/>
                        </ObjectAnimationUsingKeyFrames>
                    </Storyboard>
                </VisualState>
            </VisualStateGroup>
        </VisualStateManager.VisualStateGroups>
    </Grid>
</common:LayoutAwarePage>
